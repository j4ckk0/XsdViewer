<?xml version="1.0" encoding="UTF-8"?>
<!-- Sample schema, adapted from the W3C XML Schema Primer purchase order. -->
<xs:schema xmlns:xs="http://www.w3.org/2001/XMLSchema"
           xmlns:po="http://example.com/po"
           targetNamespace="http://example.com/po"
           elementFormDefault="qualified">

  <xs:annotation>
    <xs:documentation>
      Purchase order schema for Example.com.
      Copyright 2000 Example.com. All rights reserved.
    </xs:documentation>
  </xs:annotation>

  <xs:element name="purchaseOrder" type="po:PurchaseOrderType">
    <xs:annotation>
      <xs:documentation>Root element of a purchase order document.</xs:documentation>
    </xs:annotation>
  </xs:element>

  <xs:element name="comment" type="xs:string"/>

  <xs:element name="urgentComment" type="xs:string" substitutionGroup="po:comment"/>

  <xs:complexType name="PurchaseOrderType">
    <xs:sequence>
      <xs:element name="shipTo" type="po:USAddress"/>
      <xs:element name="billTo" type="po:USAddress"/>
      <xs:element ref="po:comment" minOccurs="0"/>
      <xs:element name="items" type="po:Items"/>
    </xs:sequence>
    <xs:attribute name="orderDate" type="xs:date"/>
    <xs:attributeGroup ref="po:AuditAttributes"/>
  </xs:complexType>

  <xs:complexType name="USAddress">
    <xs:sequence>
      <xs:element name="name" type="xs:string"/>
      <xs:element name="street" type="xs:string"/>
      <xs:element name="city" type="xs:string"/>
      <xs:element name="state" type="xs:string"/>
      <xs:element name="zip" type="xs:decimal"/>
    </xs:sequence>
    <xs:attribute name="country" type="xs:NMTOKEN" fixed="US"/>
  </xs:complexType>

  <xs:complexType name="Items">
    <xs:sequence>
      <xs:element name="item" minOccurs="0" maxOccurs="unbounded">
        <xs:complexType>
          <xs:sequence>
            <xs:element name="productName" type="xs:string"/>
            <xs:element name="quantity">
              <xs:simpleType>
                <xs:restriction base="xs:positiveInteger">
                  <xs:maxExclusive value="100"/>
                </xs:restriction>
              </xs:simpleType>
            </xs:element>
            <xs:element name="USPrice" type="xs:decimal"/>
            <xs:element ref="po:comment" minOccurs="0"/>
            <xs:element name="shipDate" type="xs:date" minOccurs="0"/>
            <xs:group ref="po:ItemExtras" minOccurs="0"/>
          </xs:sequence>
          <xs:attribute name="partNum" type="po:SKU" use="required"/>
        </xs:complexType>
      </xs:element>
    </xs:sequence>
  </xs:complexType>

  <xs:group name="ItemExtras">
    <xs:sequence>
      <xs:element name="giftWrap" type="xs:boolean"/>
      <xs:element name="giftMessage" type="po:Message"/>
    </xs:sequence>
  </xs:group>

  <xs:attributeGroup name="AuditAttributes">
    <xs:attribute name="createdBy" type="xs:string"/>
    <xs:attribute ref="po:version"/>
  </xs:attributeGroup>

  <xs:attribute name="version" type="xs:token"/>

  <!-- Stock Keeping Unit, a code for identifying products -->
  <xs:simpleType name="SKU">
    <xs:annotation>
      <xs:documentation>Stock Keeping Unit, e.g. 123-AB.</xs:documentation>
    </xs:annotation>
    <xs:restriction base="xs:string">
      <xs:pattern value="\d{3}-[A-Z]{2}"/>
    </xs:restriction>
  </xs:simpleType>

  <xs:simpleType name="Message">
    <xs:restriction base="po:LimitedText"/>
  </xs:simpleType>

  <xs:simpleType
      name="LimitedText">
    <xs:restriction base="xs:string">
      <xs:maxLength value="200"/>
    </xs:restriction>
  </xs:simpleType>

  <xs:simpleType name="SKUList">
    <xs:list itemType="po:SKU"/>
  </xs:simpleType>

  <xs:simpleType name="Identifier">
    <xs:union memberTypes="po:SKU xs:positiveInteger"/>
  </xs:simpleType>

  <xs:complexType name="InternationalAddress">
    <xs:complexContent>
      <xs:extension base="po:USAddress">
        <xs:sequence>
          <xs:element name="countryName" type="xs:string"/>
        </xs:sequence>
      </xs:extension>
    </xs:complexContent>
  </xs:complexType>

  <!-- A recursive type: a category may contain sub-categories -->
  <xs:complexType name="Category">
    <xs:sequence>
      <xs:element name="subCategory" type="po:Category" minOccurs="0" maxOccurs="unbounded"/>
    </xs:sequence>
    <xs:attribute name="label" type="ext:Label"/>
  </xs:complexType>

</xs:schema>
